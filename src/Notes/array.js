// Array methods - methods that you can call on any array that will perform some kind of action on that array or with that array
//every array method takes in a different kinda argument

const myArray = [1,2,3,4];

map();

filter();

reduce();

find();

includes();